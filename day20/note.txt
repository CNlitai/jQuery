1.$() 来调用jq函数
   参数：匿名函数 $(function() {

   })

        css选择器 代表选中某个节点
          $('div')
          $(.'one')

        html的片段
          append 
          $('div').append($('<span>'))

        element元素
          var $a = $('span');
           $('div').append($('$a'))


2.jquery方法都是做批量操作



3.选择器
  基本选择器
  层次选择器
  过滤器
    :first
    :last
    :eq(index)
    :odd
    :even
    :lt(index)    索引小于index
    :gt(index)
    :header  选中标题


4.内容过滤器



  